{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
</br>
</br>
</br>


    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ service.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ service.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_service_index') }}">back to list</a>

    <a href="{{ path('app_admin_service_edit', {'id': service.id}) }}">edit</a>

    {{ include('admin_service/_delete_form.html.twig') }}
{% endblock %}
